<%# Dùng: include('partials/icon', { name: 'trophy' }) — tên là id trong partials/icons.ejs, bỏ tiền tố "i-". %><svg class="ic<%= typeof cls !== 'undefined' && cls ? ' ' + cls : '' %>" aria-hidden="true"><use href="#i-<%= name %>"/></svg>