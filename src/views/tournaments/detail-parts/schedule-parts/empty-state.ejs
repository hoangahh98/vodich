<% if (!matches.length) { %>
  <div class="empty-state">
    <%- include('../../../partials/icon', { name: 'court' }) %>
    <p class="muted">Chưa có trận thi đấu.<% if (currentUser.role === 'ADMIN') { %> Bấm "Chia trận" ở trên để chia.<% } %></p>
  </div>
<% } %>
