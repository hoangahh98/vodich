<section class="feature-hero p mb">
  <div class="flex between center">
    <div>
      <div class="hero-eyebrow">Đội bóng</div>
      <h1><%= team.name %></h1>
      <div class="muted strong-muted"><%= team.description || 'Chưa có ghi chú' %></div>
    </div>
    <div class="team-hero-tools">
      <form method="get" action="/teams/<%= team.id %>/<%= section %>" class="month-filter">
        <label>Tháng quỹ</label>
        <input type="month" name="month" value="<%= selectedMonth %>" data-autosubmit>
      </form>
      <% if (currentUser.role === 'ADMIN') { %>
        <button class="btn btn-sm btn-success" type="button" data-open-modal="teamFundReportModal"><%- include('../../partials/icon', { name: 'receipt' }) %> Báo quỹ</button>
      <% } %>
    </div>
  </div>
</section>
