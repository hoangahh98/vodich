.idea/
.vscode/
target/
.pytest_cache/
__pycache__/
*.iml
.env
# Mọi file .env (kể cả bản tải từ Render về, ví dụ pickleball.env): chứa DATABASE_URL,
# SESSION_SECRET, token bot, PAT GitHub — repo public, lọt lên là phải đổi sạch key.
*.env
.DS_Store
*.log
node_modules
dist
playwright-report/
test-results/
.e2e-state.json
.codex/
backups/
san_pick.png

# Ảnh mail ngân hàng mẫu của chủ app (có số tài khoản, tên) — không đưa lên repo public.
thetindung.PNG
khoanchi.PNG
timonhantien.PNG
timotrutien.PNG
