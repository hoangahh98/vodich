services:
  - type: web
    name: vodich-tool
    env: node
    plan: free
    buildCommand: npm run render:build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
